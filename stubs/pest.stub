<?php

declare(strict_types=1);

use Tests\TestCase;

describe('Example feature test', function (): void {
    it('asserts the application returns a successful response', function (): void {
        /** @var TestCase $this */

        // $this->withoutExceptionHandling();

        $response = $this->get('/');

        $response->assertStatus(200);
    });
});
